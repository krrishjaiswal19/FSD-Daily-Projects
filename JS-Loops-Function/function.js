// Normal Function

function add(a, b) {
  return a + b;
}

console.log(add(5, 3)); // 8


// Breakdown:

// a, b → parameters

// return → result bhejta hai

// Arrow Function (Short & Modern)
const add = (a, b) => {
  return a + b;
};