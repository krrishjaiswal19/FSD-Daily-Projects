// Normal
function multiply(a, b) {
      return a * b;
      }

// Arrow function
const multiply = (a, b) => a * b;

// Template Literal Output
let product = "Laptop";
let price = 50000;

console.log(`Product: ${product}, Price: ₹${price}`);

/*
Small Project – ES6 Practice File
👉Include 10 examples, like:
let vs const
Arrow function
Template literal
Default param
Arrow + array map
Object with const
Multi-line string
Arrow function return
Default param calc
Clean console output
*/ 